# codeup-kiro

在阿里云 Codeup 的合并请求（MR）上运行自动代码评审，并把结果以评论形式写回 MR。
本文件只是词汇表：定义这个项目里各个概念**是什么**，不记录实现方式与决策（决策见 `docs/adr/`）。

## Language

### 评审对象与身份

**业务库**:
被评审的 Codeup 代码库。其内容（包括其中的脚本与配置）一律视为不受信数据。
_Avoid_: 目标仓库、被评审仓库、源仓库

**集成包**:
本仓库。提供评审所需的脚本、提示词与配置，是唯一受信的代码来源。
_Avoid_: 脚本仓库、工具库

**评审员**:
一个自动化的评审身份，对一次评审产出一份评审报告。目前有两位：Kiro 评审员、Security Agent 评审员。
_Avoid_: 机器人、bot、agent（后者在本项目里另有所指：Kiro 的 custom agent 配置）

**机器人账号**:
评审员在 Codeup 上发评论所用的用户身份。每位评审员对应一个独立的机器人账号，评论的作者名与头像即来自它。
_Avoid_: 服务账号、令牌账号

### 评审产出

**评审**:
对一个 MR 在某个提交上的一次完整自动评审过程，由一位评审员完成，产出一份评审报告。
_Avoid_: 扫描（保留给 Security Agent 的差分扫描）、检查、审查

**评审报告**:
评审员对一次评审的结构化产出：若干条问题加一个总体结论。
_Avoid_: 评审结果、输出

**问题**:
评审报告中的一条独立意见，带严重级别与说明，通常指向具体文件与行。
_Avoid_: 缺陷、issue、finding（仅在直接引用 Security Agent API 字段时使用）、评论项

**严重级别**:
问题的分级：P0（必须修复：安全漏洞、数据丢失、逻辑错误）、P1（应当修复：性能、并发、错误处理）、P2（可选改进：规范、可读性）。
_Avoid_: 严重/警告/建议、红黄蓝灯、优先级、Critical/Major/Minor、riskLevel（仅在直接引用 Security Agent API 字段时使用）

**总体结论**:
评审报告末尾对整个 MR 的评价与合并建议（可合并 / 建议修改后合并 / 不建议合并）。
_Avoid_: 总结、评分

**可定位问题**:
所指行落在本次 MR 变更行集合内、因而可以作为行内评论发出的问题。
_Avoid_: 有效问题

**未定位问题**:
所指行不在变更行集合内（或未给出行号）的问题，只能进入汇总评论。
_Avoid_: 无效问题、幻觉问题

### 评论

**汇总评论**:
挂在 MR 评论区的那一条评论，承载元信息、各级别问题计数、总体结论与未定位问题。每次评审至多一条。
_Avoid_: 全局评论、整体评论、总评、GLOBAL_COMMENT（仅在直接引用 Codeup API 字段时使用）

**行内评论**:
锚定到某个文件某一行、且绑定一对版本的评论，承载一条可定位问题。
_Avoid_: 行评论、代码评论、INLINE_COMMENT（仅在直接引用 Codeup API 字段时使用）

**版本**:
Codeup 为 MR 的合并源或合并目标在某一时刻记录的快照，行内评论必须指明比较的起止版本。
_Avoid_: 补丁集、patch set（仅在直接引用 Codeup API 字段时使用）、快照

**折叠区**:
汇总评论中默认收起的部分，承载未发行内的问题（P2、超出上限的 P0/P1、未定位问题）。
_Avoid_: 附录、更多问题

**行内档位**:
决定哪些严重级别的可定位问题以行内评论发出的策略：quiet（P0+P1）、balanced（全部）、critical（仅 P0）。
_Avoid_: 严格度、strictness、模式

**评审标记**:
嵌在汇总评论中的隐藏标识，记录该评审对应的提交，用于识别与去重。
_Avoid_: 水印、签名

### 触发与执行

**触发事件**:
使一次评审开始的 Codeup 事件：MR 新建、MR 代码推送更新、评论命令。
_Avoid_: 钩子、webhook（那是传递事件的机制，不是事件本身）

**评论命令**:
写在 MR 评论里、用于要求评审员重新评审的固定短语（如 `/kiro review`）。
_Avoid_: 斜杠命令、指令（本项目中"指令"指提示词对模型的要求）

**调度器**:
接收触发事件、判定是否需要评审并发起一次评审的组件。
_Avoid_: 触发器、网关、入口

**执行器**:
实际运行一次评审的计算环境。
_Avoid_: 构建机、执行机、runner、worker

**部署档位**:
集成包的一种完整部署形态，决定调度器与执行器由谁承担。目前有 Flow 档位与 AWS 档位。
_Avoid_: 模式、方案、profile

**差分扫描**:
Security Agent 评审员针对一份 diff、以全库上下文为背景所做的安全分析。
_Avoid_: 增量扫描、diff scan

**全库上下文**:
供差分扫描参考的业务库完整源码副本。
_Avoid_: 代码快照、归档
